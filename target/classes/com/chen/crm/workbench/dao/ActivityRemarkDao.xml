<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chen.crm.workbench.dao.ActivityRemarkDao">
<update id="updateRemark">
	update tbl_activity_remark set
	noteContent = #{noteContent},editBy = #{editBy},editTime = #{editTime},editFlag = #{editFlag}
	where id = #{id}
</update>

<insert id="saveRemark">
	insert into tbl_activity_remark(
		id,
		noteContent,
		createTime,
		createBy,
		editFlag,
		activityId
	)
	values(
		#{id},
		#{noteContent},
		#{createTime},
		#{createBy},
		#{editFlag},
		#{activityId}
	)
</insert>

<delete id="deleteRemarkById">
	delete from tbl_activity_remark where id=#{id}
</delete>

<select id="getRemarkListByAid" resultType="com.chen.crm.workbench.domain.ActivityRemark">
	select * from tbl_activity_remark where activityid = #{activityId};
</select>

<!-- 对传过来的数组进行组装成sql语句 -->
	<select id="getCountByAids" resultType="int">
		select count(*) from tbl_activity_remark
		where activityId in
		<foreach collection="array" open="(" close=")" item="aid" separator=",">
			#{aid}
		</foreach>
	</select>
	
	<!-- 进行删除操作 -->
	<delete id="deleteByAids">
		delete from tbl_activity_remark
		where activityId in
		<foreach collection="array" open="(" close=")" item="aid" separator=",">
			#{aid}
		</foreach>
	</delete>
</mapper>